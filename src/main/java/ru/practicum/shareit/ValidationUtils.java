package ru.practicum.shareit;

import org.springframework.http.HttpStatus;
import org.springframework.web.server.ResponseStatusException;

//public class ValidationUtils {
  //  public static void validatePaginationParameters(Integer from, Integer size) {
    //    if (from == null && size == null) return;
      //  if (from == null || size == null || from < 0 || size < 1) {
        //    throw new ResponseStatusException(HttpStatus.BAD_REQUEST, "from or size = null");
        //}
   // }
//}
